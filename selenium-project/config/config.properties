
# All settings read from here. Nothing hardcoded in code.


# Application
base.url=http://demo.testfire.net

# Browser settings
# Options: chrome, firefox, edge
browser=chrome
headless=false

# Timeouts (in seconds)
implicit.wait=10
explicit.wait=15
page.load.timeout=30

# Altoro Mutual demo credentials
# These are public demo credentials built into the app
app.username=jsmith
app.password=Demo1234

# Test environment
environment=staging